// SPDX-License-Identifier: MIT
// Redirect TU — implementation lives in src/util/ProfileManager.cpp
// This .cpp file is listed in CMakeLists.txt under
// src/profile/ProfileManager.cpp. Including the util header satisfies the
// compiler; the linker sees one TU.
#include "../util/ProfileManager.cpp"
